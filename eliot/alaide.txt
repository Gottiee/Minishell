commancer la commande avec un pipe

TRUC A FAIRE

- appeller les builtins 
	- il faut sauvegarder dans l'env le chemin actuell pour que quand pwd marche pas, qu'il puisse se retrouver fonction: get_current_path;
	- retester pwd quand on est perdu lorsque j'aurais implementer le pwd qui met .
	- tester les autres builtins pas encore coder (env/ unset)

- test: 
	- tester le pipex finis
	- tester cd
	- tester pwd


- valeur de retour: 
$? 
	- mettre la valeur de retoru dans l'env dans la fonction parsing de theo
	- ctrl c sur le here doc
	- gerer les valeur de retour des builtins qui rate 
	- no such or directory c'est 1
	
- norm

- malloc

- fds

- bouger le cd


DONE today
	- quand une commande n'est pas trouve il renvoie 1
	- quand une commande est un builtin renvoie 1 egalement
	- erreur quand on tape << < >> bizare
	- ctr-d segfault sur le parsing de theo
	- segfault quand: |  solo
